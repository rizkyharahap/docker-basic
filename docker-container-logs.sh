# Log container
# required container_name/container_id
docker container logs redisexample

# Log container with follow/realtime
# required container_name/container_id and -f
docker container logs -f redisexample